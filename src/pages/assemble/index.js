import React, {Component} from 'react';

export default class Assemble extends Component {
    render() {
        return (
            <div>
                Assemble
            </div>
        );
    }
}

